/**
  * Obtained from https://github.com/editdb/Pebble-Math
  * Modified to include only necessary functions
  */

#pragma once

float sm_sqrt(const float);
float sm_powint(float, int);
float sm_acosd(float);

float sm_asin(float);
float sm_asind(float);
float sm_atand(float);
float sm_acos(float);
float sm_atan(float);